query FetchRD($pilot_nuts2: String!) {
  datapoints(
    survey: "RD_E_GERDREG"
    sortBy: ["year", "value"]
    sortOrder: ["desc", "desc"]
    dimensions: [$pilot_nuts2, "All sectors", "Million euro"]
  ) {
    region
    source
    timestamp
    survey
    dimensions
    value
  }
}
