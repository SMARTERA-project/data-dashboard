query FetchCoolingDays($pilot_nuts3: String!) {
  datapoints(
    survey: "NRG_CHDDR2_A"
    sortBy: ["year", "value"]
    sortOrder: ["desc", "desc"]
    dimensions: [$pilot_nuts3, "Cooling degree days"]
  ) {
    region
    source
    timestamp
    survey
    dimensions
    value
  }
}
