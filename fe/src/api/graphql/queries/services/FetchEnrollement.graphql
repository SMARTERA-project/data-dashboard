query FetchEnrollement($pilot_nuts2: String!) {
  datapoints(
    survey: "EDUC_UOE_ENRT06"
    sortBy: ["year", "value"]
    sortOrder: ["desc", "desc"]
    dimensions: [$pilot_nuts2, "Total"]
    limit: 6
  ) {
    region
    source
    timestamp
    survey
    dimensions
    value
  }
}
