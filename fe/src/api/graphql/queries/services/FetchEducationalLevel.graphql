query FetchEducationalLevel($pilot_nuts2: String!) {
  datapoints(
    survey: "EDUC_UOE_ENRA14"
    sortBy: ["year", "value"]
    sortOrder: ["desc", "desc"]
    dimensions: [$pilot_nuts2, "From 15 to 24 years"]
  ) {
    region
    source
    timestamp
    survey
    dimensions
    value
  }
}
