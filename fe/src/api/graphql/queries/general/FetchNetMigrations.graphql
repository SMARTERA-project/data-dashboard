query FetchNetMigrations($pilot_nuts3: String!) {
  datapoints(
    survey: "demo_r_gind3"
    sortBy: "year"
    sortOrder: "asc"
    dimensions: [$pilot_nuts3, "Net migration plus statistical adjustment"]
  ) {
    region
    source
    timestamp
    survey
    dimensions
    value
  }
}
